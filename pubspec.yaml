name: asflutter
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # Networking
  http: ^1.2.2

  # UI & Widgets
  cupertino_icons: ^1.0.8
  loading_animation_widget: ^1.2.1
  flutter_colorpicker: ^1.1.0
  intl: ^0.19.0   # <--- for DateFormat (fixes your earlier error)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/logo.png
    - assets/images/   # <--- add folder so all icons/images in assets/images work
